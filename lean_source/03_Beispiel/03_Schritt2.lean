
/- TEXT:

Schritt 2: Werte der Ableitungen am Entwicklungspunkt berechnen
===============================================================

Da LEAN die Werte selbst berechnen kann, können wir uns diesen Schritt sparen. 

Wie in den *#eval* Statements schon gesehen, wird um eine Funktion/Definition mit einem Eingangsparameter zu berechnen, zuerst der Name der Funktion, dann ein Leerzeichen und dann die Zahl, die den Eingangsparameter darstellt geschrieben. 
Also z.B. *f' 0* bedeutet soviel wie *f'(0)*.


TEXT. -/