import data.real.basic


/- TEXT:

Dieses Buch thematisiert das Taylorpolynom. Es soll erst formal definiert werden und dann mit LEAN hergeleitet und beispielhaft berechnet werden.
Die Schritte zur Erstellung des Taylorpolynoms sind aufgezeigt und werden an einem Beispiel ausprobiert.
Der LEAN Code für das Beispiel kann mithilfe des *Try it!* Buttons in ein LEAN File kopiert und dort ausprobiert werden.



TEXT. -/


